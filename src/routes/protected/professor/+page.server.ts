import type { PageServerLoad } from "./$types";

export const load: PageServerLoad = async ({ locals }) => {

    // TODO:
    // COLOCAR AS CHAMADAS DE API DO BACKEND AQUI PARA RETORNO
    // DE INFORMAÇÕES PERTINENTES DA PAGINA
    // CHAMADAS ESSAS QUE DEVEM RETORNAR OS DADOS REFERENTES
    // Á HOME DO PROFESSOR

    return locals
}